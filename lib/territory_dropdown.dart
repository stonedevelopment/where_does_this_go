import 'package:flutter/material.dart';

class TerritoryDropdown extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return DropdownButton(
      items: <DropdownMenuItem>[],
      onChanged: (value) {},
    );
  }
}
